<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <group  android:checkableBehavior="single">
        <item
            android:checked="true"
            android:id="@+id/sort_to_oldest_first"
            android:title="@string/new_to_old" />
        <item
            android:id="@+id/sort_to_latest_first"
            android:title="@string/old_to_new" />
    </group>

    <!-- action button for refresh -->
    <item android:title="@string/refresh"
        android:id="@+id/refresh"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"
        android:icon="@drawable/icon_refresh"/>
</menu>